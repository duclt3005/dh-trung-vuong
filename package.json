{
  "name": "trung-vuong-university",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "sass-dev": "sass ./scss:styles",
    "sass-watch": "sass --watch --embed-source-map ./scss:styles",
    "sass-prod": "sass --no-source-map --style compressed ./scss:styles",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "duclt",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.69.5"
  }
}
